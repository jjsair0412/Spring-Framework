<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns = "http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd">
		
		<!-- @Autowired를 사용하기 위해서, <context:annotation-config/> 태그 를 추가해주어야 하는데, 
		해당 태그를 사용하기 위해서는
		최상단에 xmlns:context="~~"부분을 추가해주어야 한다. 
		또한 xsi쪽 부분도 추가해주어야한다.-->
		<context:annotation-config />
		
		<!-- 원래는 constructor-arg 태그를 사용해, custom생성자에 파라미터값으로 객체를 넣어주어야 했엇다.
		하지만, 의존객체 자동주입 방법 중, @Autowired를 사용한다면, 
		주입하려 하는 객체의 타입이 일치하는 객체를 컨테이너에서 찾아 자동으로 주입한다.
		ex ) String타입이면 String타입의 객체를 넣는다. 이름은 상관이없다. 객체타입만 보고 자동으로 주입한다. -->
		
		<bean id = "Autowired_class" class = "Auto_Di_Sutdy.Autowired_class"/>
		<bean id = "Resource_class" class = "Auto_Di_Sutdy.Resource_class"/>
		
		<bean id = "personName1" class = "Auto_Di_Sutdy.PersonName1"/>
		<bean id = "personName2" class = "Auto_Di_Sutdy.PersonName2"/>
</beans>